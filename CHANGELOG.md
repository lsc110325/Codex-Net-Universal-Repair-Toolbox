# 更新日志 / Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)；`v1.0.100` 之前为公开测试版（bate）。

## v1.0.200

- 界面动画：开关滑块滑动、按钮悬停/按下过渡、导航滑块（**圆角矩形** + ease-in-out）
- 导航改为**父级统一绘制 + 命中测试**，修复"文字与滑块消失"的问题
- 最小化 / 关闭改为**纯图标**，悬停改为**半透明**高亮（不再刺眼）
- 更新日志新增「**作者有话说**」并固定置顶（不随版本列表滚动）
- 导航选中态与最小化 / 关闭统一为**白色圆角矩形**（文字 / 图标用强调色），风格更统一
- 修复：更新日志版本卡片被下方卡片遮挡（下半截与圆角看不见）——列表加高、历史卡下移；卡片圆角加大到 16
- 新增【**国内网络直连**】开关：把 .cn 与 30 个国内常用域名写入 NO_PROXY，国内站点直连不走代理（境外仍走代理）
- 新增主界面【**打开 Codex**】按钮（不结束进程，直接拉起 Codex 桌面应用）
- 版本号升级为 **v1.0.200**

## v1.0.125

- 新增**界面动画**：开关心爱滑块滑动 + 轨道颜色渐变、按钮悬停/按下过渡、导航胶囊**滑动切换**
- 新增设置项【界面动画】开关（关闭后立即变为无动画的即时切换，适合低配机器）
- 导航滑块改为**圆角矩形**（radius 8，非两端半圆），切换缓动改为 ease-in-out 更自然
- 文字颜色随滑块覆盖比例渐变，消除切换过程中“文字先变色 / 边框露馅”的问题
- 修复：导航栏文字与滑块消失（子控件背景擦除父级绘制）——改为无子控件的父级统一绘制 + 命中测试
- 细节：右上角最小化 / 关闭改为**纯图标**（去掉白色方块背景），悬停时才淡高亮
- 修复：点击导航只切换页面、白色胶囊不跟随选中的问题

- 更新日志页新增「**作者有话说**」分区，并**固定在页面顶部**（不随版本列表滚动，未来新增版本不受影响）
- 分区内容：作者的话 + 技术反馈 QQ 群：783904560
- 版本标识修正：`v1.x` 显示为正式版，`v26.9.26.*` 显示为测试版（bate）
- 版本号升级为 v1.0.125

## v1.0.100（正式版）

- 界面：导航栏与红色更新提示改为**完整圆弧圆角胶囊**
- 修复：胶囊圆角被内部 Label 遮挡（改为在圆角面板上直接绘制文字）
- 修复：导航选中胶囊不显示（改为在顶部栏绘制）
- 修复：版本日志数组长度不匹配导致的启动异常
- 修复：修复历史 `history.log` 改为 UTF-8 BOM，记事本/编辑器不再乱码
- 新增：`tools/stress-test.ps1`（38 项极端场景测试）
- 验证：托盘常驻关闭行为（真实点击 ✕ 最小化到托盘）
- 版本号正式定为 **v1.0.100**

## v26.9.26.300bate

- 目录结构整理：根目录放本体，代码 / 图库 / 工具分英文文件夹
- 更新提示支持【不再提醒】，主界面保留**红色胶囊**提醒
- 全部版本统一标注 bate 并保留历史日志

## v26.9.26.285bate

- 修复「最近修复记录」被重复自检刷屏
- 历史记录支持连续合并计数（×N）与一键清空
- 记录卡片加高、长文本不再截断

## v26.9.26.280bate

- 修复辅助色串色（顶部栏 / 导航未跟随配色）
- 导航改为胶囊样式，支持拖动窗口
- 界面细节打磨，更新说明精简

## v26.9.26.200bate

- 升级全新 UI 与界面风格
- 移除深色模式，彻底消除黑边
- 首次启动提示条、清理旧版本、支持覆盖升级

## v26.9.26.050bate

- 白色圆角现代界面，四种辅助色
- 修复卡片内控件黑色描边
- 主界面重排、独立文件夹交付

## v26.9.26.010bate

- 系统托盘常驻 + 代理健康监控
- 一键重启 Codex、启动时自动自检
- 修复历史、复制诊断报告、命令行脚本化

## v26.9.26.001bate（首个版本）

- 一键修复并部署（代理配置 / 用户环境变量 / git TLS 后端）
- 代理端口自动探测（TCP + CONNECT + 真实出网验证）
- 修复沙箱内 `git clone` 报 `remote-https` 缺失
- 修复沙箱内 git/curl 报 `SEC_E_NO_CREDENTIALS`
- 11 项全面自检 + 备份还原